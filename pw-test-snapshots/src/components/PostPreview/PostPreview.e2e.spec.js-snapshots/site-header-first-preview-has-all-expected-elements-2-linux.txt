Let's filter out Playwright test runs traffic from Google Analytics. We will use Playwright's configuration file to set up a debug_mode variable on browser's localStorage to be picked up by Google Analytics Configuration. This way we can run our tests in any environment and from any IP and be certain that Google Analytics data is not affected by the test traffic.