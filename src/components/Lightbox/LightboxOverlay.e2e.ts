export enum LightboxOverlaySelector {
  host = '[data-testid=Lightbox-Portal-Content]',
  image = '.ril-image-current:not(.ril-not-loaded)',
  closeButton = '.ril__closeButton',
  title = '[data-testid=LightboxImageTitle-Title]',
  indexOf = '[data-testid=LightboxImageTitle-IndexOf]',
  toolbar = '.ril__toolbar',
}
